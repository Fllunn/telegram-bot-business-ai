- Виртуальная среда: `python -m venv .venv && source .venv/Scripts/activate` (Windows: `python -m venv .venv && .venv\\Scripts\\activate`)

- Зависимости: `pip install -r requirements.txt`

- Запуск: `python -m src.main`